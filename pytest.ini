[pytest]
markers =
    integration: uses cluster DuckDB + parquet paths (slow).
    slow: exercises a real engine subprocess (e.g. Stockfish); opt-in, may take minutes.
testpaths = src/analysis/tests
pythonpath = src
