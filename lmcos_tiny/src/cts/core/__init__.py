"""Shared substrate used by every other CTS subpackage.

Includes the in-memory ``SearchTree`` representation, the tensorizer that
projects trees into batched GPU tensors, the canonical feature schema, and
the engine/provider layer for talking to lc0.
"""
