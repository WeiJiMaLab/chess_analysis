"""Neural-network modules used across CTS.

- ``cts.models.gnn``      — tree encoder (``TreeEncoder`` + slot-conditioned attention) and child-WDL heads
- ``cts.models.tree_mha`` — slot-conditioned multi-head attention block used inside the encoder
- ``cts.models.mc``       — meta-controller (``MetaController``: encoder + advantage head)
"""
